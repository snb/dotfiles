Various config files that I usually want in my home directory. Clone it as
$HOME/.dotfiles:

    cd ~
    git clone https://github.com/snb/dotfiles.git

Then make symlinks (this doesn't handle existing dotfiles with the same names
yet).

    .dotfiles/makelinks.sh
